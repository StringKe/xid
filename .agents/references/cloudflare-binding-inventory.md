---
description: "The exact binding names declared in wrangler.jsonc - D1, the eight Durable Object classes, the six queues with their consumer settings and message shapes, and the two cron expressions with their job lists"
std-agent-type: references
---
<!-- Generated by stdagent 0.0.10. Source: .stdai/standards/references/cloudflare-binding-inventory.md. Run `stdagent sync` to regenerate. Do not edit by hand. -->
<!-- std-agent degraded references: cloudflare-binding-inventory -->
<!-- Target tool "codex" does not natively support this type. Reference is background material; AI consults it only when needed, not auto-loaded. -->


# Cloudflare binding inventory

Lookup tables extracted from the `cloudflare-bindings` rule. Read this when you need the literal name
of a binding, queue, Durable Object class or cron expression, or the shape of a queue message. The
binding names below are the ones declared in `apps/server/wrangler.jsonc` and typed in
`packages/types/src/env.ts` -- use those names, never invent new ones. The judgment calls about which
service to pick for a given job stay in the `cloudflare-bindings` rule.

## Service Mapping (bindings as declared)

| Service           | Binding                                                                                            | Purpose                                                                                       |
| ----------------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| Workers + Hono    | `main = worker/index.ts`                                                                           | HTTP and protocol handling; non-API paths fall back to SPA assets via `ASSETS`                 |
| D1                | `DB` (`xid-db`)                                                                                    | Users, applications, credential metadata, authorization codes, refresh tokens, audit, tenants, key ciphertext, sessions |
| Durable Objects   | 8 bindings (see table below)                                                                       | Strong consistency, replay protection, serialized writes                                       |
| KV                | `CACHE`                                                                                            | JWKS / discovery / branding config / feature flags                                             |
| R2                | `STORAGE`                                                                                          | Org logos and email locale packs (see status note)                                             |
| Queues            | 6 producers + `xid-dlq` dead letter queue                                                          | Email, SMS, WhatsApp, audit persistence, webhook delivery, metering                            |
| Email Sending     | `EMAIL` (`send_email`)                                                                             | Cloudflare Email Service outbound transactional mail; sends to arbitrary external addresses    |
| Cron Triggers     | `0 * * * *` hourly, `0 2 * * *` daily                                                              | Expiry cleanup, key rotation, certificate/domain polling, DAU/MAU aggregation                  |
| Workers Secrets   | `KEK`, `PEPPER`, provider credentials (`TWILIO_*`, `VONAGE_*`, `INFOBIP_*`, `MESSAGEBIRD_*`, ...)   | Envelope-encryption master key, password pepper, provider credentials                          |
| Turnstile         | `TURNSTILE_SECRET` (secret, not a binding)                                                         | Form-level bot defense via siteverify HTTP call                                                |
| Analytics Engine  | `ANALYTICS` (`xid_analytics`)                                                                      | Real-time metrics (login success, MFA adoption, active users)                                  |

There is no Cloudflare Rate Limiting binding and no WAF binding in `wrangler.jsonc`. Application-level
rate limiting runs entirely through the `RATE_LIMITER` Durable Object. Network-layer WAF and Rate
Limiting rules, if used, are configured in the Cloudflare dashboard, not in code.

## Durable Object bindings

| Binding              | Class              | Responsibility                                                                     |
| -------------------- | ------------------ | ---------------------------------------------------------------------------------- |
| `WEBAUTHN_CHALLENGE` | `ChallengeStore`   | WebAuthn / OTP / magic-link challenges, one-shot, destroyed on consume              |
| `OAUTH_STATE`        | `OAuthFlowDO`      | OAuth state / nonce / PKCE plus parked `/authorize` params for unauthenticated users |
| `PAR_STORE`          | `ParStore`         | RFC9126 PAR, `request_uri` valid 60s, single use, DO name = `tenantId`              |
| `DEVICE_FLOW`        | `DeviceFlowStore`  | RFC8628 device flow, `device_code` and `user_code` stored separately, poll throttling |
| `SESSION_REVOCATION` | `SessionDO`        | Per-user active session id set, serialized revocation                               |
| `RATE_LIMITER`       | `RateLimitStore`   | Per-tenant / account / IP rate limit counters in DO SQLite storage                  |
| `AUDIT_SEQ`          | `AuditSeqDO`       | Sole committer of the per-tenant audit hash chain                                   |
| `METERING`           | `MeteringDO`       | Per-tenant exact MAU/DAU deduplication                                              |

All eight classes are registered in the `v1` migration as `new_sqlite_classes`.

## Queues

| Producer binding  | Queue           | Consumer settings                                                      |
| ----------------- | --------------- | ---------------------------------------------------------------------- |
| `EMAIL_QUEUE`     | `xid-email`     | batch 100, timeout 5s, max_retries 5, DLQ `xid-dlq`                     |
| `WHATSAPP_QUEUE`  | `xid-whatsapp`  | batch 100, timeout 5s, max_retries 5, DLQ `xid-dlq`                     |
| `SMS_QUEUE`       | `xid-sms`       | batch 100, timeout 5s, max_retries 5, DLQ `xid-dlq`                     |
| `AUDIT_QUEUE`     | `xid-audit`     | batch 100, timeout 5s, **max_concurrency 1**, max_retries 5, DLQ `xid-dlq` |
| `WEBHOOK_QUEUE`   | `xid-webhook`   | batch 50, timeout 5s, max_retries 5, DLQ `xid-dlq`                      |
| `METERING_QUEUE`  | `xid-metering`  | batch 100, timeout 5s, max_retries 5, DLQ `xid-dlq`                     |

Message shapes live in `packages/types/src/env.ts` and are camelCase on the wire:
`EmailQueueMessage` / `SmsQueueMessage` / `WhatsappQueueMessage` are `{ type, recipient, payload }`,
`AuditQueueMessage` is `{ tenantId, orgId?, action, actorId?, ts, payload }`,
`WebhookQueueMessage` is `{ tenantId, event, payload }`,
`MeteringQueueMessage` is `{ tenantId, userId, ts }`.

## Cron Triggers

Two expressions, dispatched by `dispatchScheduled(cron, env)` in `apps/server/worker/crons/index.ts`.

- `0 * * * *` (`runHourly`): expired session cleanup, expired access-token revocation cleanup, expired
  authorization code cleanup, expired challenge cleanup, metering outbox redelivery, DAU aggregation.
- `0 2 * * *` (`runDaily`): signing key rotation check and `retire_after` backfill, custom hostname
  certificate status polling, domain verification polling, SAML IdP metadata refresh, monthly usage
  maintenance (MAU report, current-month snapshot, old-row cleanup).

Status page probing is not implemented. Do not reference it.
<!-- /Generated by stdagent -->

diff --git a/src/components/NimbusHead.astro b/src/components/NimbusHead.astro
index b55b9cbfdceee677d9e907d4e9a2d0eadea6147d..bc86ce6e8528221fd27959e4be656707f0877fe4 100644
--- a/src/components/NimbusHead.astro
+++ b/src/components/NimbusHead.astro
@@ -55,6 +55,14 @@ export interface Props {
   lastUpdated?: Date;
   /** Per-page head extras, appended after `config.head`. */
   head?: HeadElement[];
+  /** BCP 47 language for this page. Falls back to `config.locale`. */
+  locale?: string;
+  /** Open Graph `language_TERRITORY` locale for this page. */
+  openGraphLocale?: string;
+  /** Per-page OG/Twitter image alternative text. */
+  socialImageAlt?: string;
+  /** Per-page agent index override. */
+  llmsIndexUrl?: string;
   /**
    * Astro collection ID for this page — used to look up cross-version
    * alternates when versioning is configured. Pass `entry.collection`
@@ -75,6 +83,10 @@ const {
   socialImage,
   lastUpdated,
   head: pageHead = [],
+  locale: pageLocale,
+  openGraphLocale,
+  socialImageAlt,
+  llmsIndexUrl: pageLlmsIndexUrl,
   collection,
   entryId,
 } = Astro.props;
@@ -117,10 +129,11 @@ const isHiddenVersion = versionStatus?.isHidden === true;
 const llmsIndexPath =
   isHiddenVersion
     ? null
-    : collection
-      ? await getCollectionLlmsUrl(collection)
-      : "/llms.txt";
-const llmsIndexUrl =
+    : pageLlmsIndexUrl ??
+      (collection
+        ? await getCollectionLlmsUrl(collection)
+        : "/llms.txt");
+const resolvedLlmsIndexUrl =
   llmsIndexPath && Astro.site
     ? new URL(llmsIndexPath, Astro.site).href
     : llmsIndexPath;
@@ -133,7 +146,8 @@ const canonical = Astro.site
 
 const fullTitle = title !== config.title ? `${title} | ${config.title}` : title;
 const resolvedDescription = description ?? config.description;
-const lang = config.locale ?? "en";
+const lang = pageLocale ?? config.locale ?? "en";
+const ogLocale = openGraphLocale ?? lang;
 
 const isHomePage = Astro.url.pathname === "/";
 const ogType = isHomePage ? "website" : "article";
@@ -172,7 +186,7 @@ const ogImage = socialImagePath
     ? new URL(socialImagePath, Astro.site).href
     : socialImagePath
   : null;
-const ogImageAlt = config.socialImageAlt;
+const ogImageAlt = socialImageAlt ?? config.socialImageAlt;
 
 const structuredData = canonical
   ? JSON.stringify({
@@ -215,11 +229,11 @@ const shikiCssHref = `${import.meta.env.BASE_URL}_nimbus/shiki.css${import.meta.
 {markdownUrl && !isHiddenVersion && (
   <link rel="alternate" type="text/markdown" href={markdownUrl} />
 )}
-{hasSite && llmsIndexUrl && (
+{hasSite && resolvedLlmsIndexUrl && (
   <link
     rel="alternate"
     type="text/plain"
-    href={llmsIndexUrl}
+    href={resolvedLlmsIndexUrl}
     title="LLM index"
   />
 )}
@@ -234,7 +248,7 @@ const shikiCssHref = `${import.meta.env.BASE_URL}_nimbus/shiki.css${import.meta.
 <meta property="og:title" content={fullTitle} />
 <meta property="og:type" content={ogType} />
 <meta property="og:site_name" content={config.title} />
-<meta property="og:locale" content={lang} />
+<meta property="og:locale" content={ogLocale} />
 {resolvedDescription && <meta property="og:description" content={resolvedDescription} />}
 {hasSite && canonical && <meta property="og:url" content={canonical} />}
 {ogImage && <meta property="og:image" content={ogImage} />}

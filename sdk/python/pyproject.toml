[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "xid"
version = "0.1.0"
description = "XID Identity Platform - Python Server SDK"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["xid", "oidc", "jwt", "identity", "authentication"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = ["pyjwt[crypto]>=2.13.0,<3.0", "cryptography>=49.0.0,<50.0", "httpx>=0.28.1,<1.0"]

[project.optional-dependencies]
dev = ["pytest>=9.1.1", "pytest-asyncio>=1.4.0", "pytest-httpx>=0.36.2", "mypy>=2.3.0", "ruff>=0.16.0"]

[project.urls]
Homepage = "https://xid.dev"
Repository = "https://github.com/StringKe/xid"

[tool.hatch.build.targets.wheel]
packages = ["xid"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"

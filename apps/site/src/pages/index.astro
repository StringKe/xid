---
import { getEntry } from "astro:content";
import DocsEntryPage from "../components/DocsEntryPage.astro";

const entry = await getEntry("docs", "index");
if (!entry) throw new TypeError("English documentation hub is missing");
---

<DocsEntryPage entry={entry} />
